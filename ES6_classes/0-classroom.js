// Class representing a classroom
export default class ClassRoom {
  // Class constructor
  constructor(maxStudentsSize) {
    // Assign the maximum number of students to the _maxStudentsSize property
    this._maxStudentsSize = maxStudentsSize;
  }
}
